<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front" android:required="false"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK"/>
            <data android:mimeType="image/*"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT"/>
            <data android:mimeType="image/*"/>
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgroConecta"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Splash -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AgroConecta"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding -->
        <activity
            android:name=".ui.OnboardingActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- Login -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Register -->
        <activity android:name=".ui.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Forgot Password -->
        <activity android:name=".ui.ForgotPasswordActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Terms -->
        <activity android:name=".ui.TermsActivity"
            android:exported="false"/>

        <!-- Main (Comprador) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Buyer Module -->
        <activity
            android:name=".ui.BuyerModuleActivity"
            android:exported="false"/>

        <!-- Producto Detalle -->
        <activity
            android:name=".ui.ProductoDetalleActivity"
            android:exported="false"/>

        <!-- Carrito -->
        <activity
            android:name=".ui.CarritoActivity"
            android:exported="false"/>

        <!-- Mis Pedidos -->
        <activity
            android:name=".ui.MisPedidosActivity"
            android:exported="false"/>

        <!-- Perfil Cliente -->
        <activity android:name=".ui.PerfilClienteActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Mapa -->
        <activity
            android:name=".ui.MapaActivity"
            android:exported="false"/>

        <!-- Calificacion -->
        <activity android:name=".ui.CalificacionActivity"
            android:exported="false"/>

        <!-- Seguimiento -->
        <activity android:name=".ui.SeguimientoPedidoActivity"
            android:exported="false"/>

        <!-- Chat -->
        <activity android:name=".ui.ChatActivity"
            android:exported="false"/>

        <!-- Contactar Vendedor -->
        <activity android:name=".ui.ContactarVendedorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- ── ADMIN ── -->
        <activity android:name=".ui.AdminDashboardActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".ui.PerfilAdminActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".ui.UsuarioActivity"
            android:exported="false"/>

        <activity android:name=".ui.CrearUsuarioActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".ui.EditarUsuarioActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".ui.ProductosAdminActivity"
            android:exported="false"/>

        <activity android:name=".ui.AgregarEditarProductoActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".ui.ReportesActivity"
            android:exported="false"/>

        <activity android:name=".ui.EstadisticaUsuariosActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <activity android:name=".ui.EstadisticaProductosActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- ── VENDEDOR ── -->
        <activity android:name=".ui.VendedorDashboardActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".ui.PerfilVendedorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".ui.AgregarProductoVendedorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAkKvBXZo39gk1fqnxVJ7PCd0UVECRT7kI"/>

        <!-- FCM -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="canal_productos"/>

        <service
            android:name=".service.AgroConectaFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <!-- Soporte Técnico -->
        <activity android:name=".ui.SoporteActivity"
            android:exported="false"/>

        <!-- FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>

</manifest>
